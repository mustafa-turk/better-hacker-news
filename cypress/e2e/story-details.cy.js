describe('story details', () => {
  it('should redirect to details page', () => {
    cy.visit('/1');

    // const url = cy.contains('some-url.com');
    // url.click();

    // cy.location('host').should('eq', 'some-url.com');
  });
});
